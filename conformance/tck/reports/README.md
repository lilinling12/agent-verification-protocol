# Conformance Reports

TCK runners emit reports that validate against `../schemas/conformance-report.schema.json`.

Reports are execution artifacts, not normative specifications. A report identifies the TCK revision and implementation under test so a result can be reproduced or audited later.

The example report is intentionally non-certifying; it demonstrates only the machine contract.
