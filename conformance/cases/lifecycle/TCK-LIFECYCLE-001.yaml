id: TCK-LIFECYCLE-001
requirement: AVP-CORE-001
type: positive
purpose: Verify an Episode exposes exactly one active lifecycle state.
input:
  episode:
    id: episode-example
    state: RUNNING
expected:
  result: pass
  invariant:
    - exactly_one_state
